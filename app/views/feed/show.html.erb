<h1>I'm a show page.</h1>

  <thead></thead>
  <tbody>
    <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
      <td><%= post.timestamp %></td>
      <td></td>
    </tr>
    <% end %>
  </body>
